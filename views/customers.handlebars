<h1>Customers:</h1>
<br>
<table class="table table-striped table-hover">
  <thead>
    <tr class="table-success">
      <th scope="col">Customer Id</th>
      <th scope="col">Password</th>
      <th scope="col">Customer Type</th>
      <th scope="col">Customer Name</th>
      <th scope="col">Phone</th>
      <th scope="col">Admin</th>
      <th scope="col"></th>
    </tr>
  </thead>
  <tbody>

      {{#each data_rows}}
        <tr>
          <form method="post">
            <td>{{this.customer_id}}</td>
            <td><input type="text" name='password' value={{this.password}}></td>
            <td><input type="text" name='customer_type' value={{this.customer_type}}></td>
            <td><input type="text" name='name' value={{this.name}}></td>
            <td><input type="text" name='phone' value={{this.phone}}></td>
            <td><input type="text" name='is_admin' value={{this.is_admin}}></td>
            <td>
                <input type='hidden' name='customer_id' value = {{this.customer_id}}>
                <input type='submit' name='EditRow' value ='Update'>
                <input type = 'submit' name = 'DeleteRow' value='Delete'>
            </td>
          </form>
        </tr>
      {{/each}}
    <form method="post">
      <tr>
        <td></td>
        {{#each column_list}}
        <td><input type="text" name={{this.Field}} id={{this.Field}}></td>
        {{/each}}
        <td><input type = 'submit' name = 'AddRow' value='AddRow'></td>
      </tr>
    </form>
  </tbody>
